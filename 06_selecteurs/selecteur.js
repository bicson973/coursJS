// FEUILLE JS - LES SELECTEURS - SEMA 

window.onload = function(){ /* avec window.onload toute la page est chargée et cela permet de mettre le script ans head sans async ou defer */
    var select1 = titreN1;
    console.info(select1);
}

