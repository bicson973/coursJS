// FICHIER JS EXTERNE - SEMA

// Création de 3 variables qui ne stockent pas de valeurs

let prenom;
console.log(prenom);  

var ville;

prenom = 'jean'; 
// Le signe égal '=' est un opérateur d'affectation. Cette variable contient une chaine de caractères.

let monAge; 
monAge = 40; 
// Cette variable contient un nombre

nom = 'kirikou';
var nom; 
console.log(nom);
// Ceci fonctionne avec var, on initialise la variable après affectation

// vehicule = 'autobus';
// let vehicule;
// console.log(vehicule);
// Ceci ne fonctionne pas avec let, il faut initialiser la variable avant affectation

var chien = 'rex';
console.log(chien);

var chien = 'kiki';
console.log(chien);

let chat ='minet';
console.log(chat);

if(true){
    let animal = 'tigre';
    console.log(animal);
}

if(true){
    var objet ='lampe';
    console.log(objet);
}

console.log(objet);
